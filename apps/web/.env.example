# Base URL of the FitCore API (NestJS backend in apps/api).
# Leave EMPTY to run the frontend in standalone demo mode (mock data + role picker).
# Set it to enable real authentication against the backend, e.g.:
# VITE_API_URL=http://localhost:3000
VITE_API_URL=
